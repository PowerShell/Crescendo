function New-DockerProxy
{
    [CmdletBinding()]
    param (
        # if non-empty - save generated module to this path
        [Parameter(Mandatory=$False)][string] $SaveToPath
    )

    "Hello from New-DockerProxy"
}
